<%= render 'header' %>

<div class="article">
  <h1><%= @article.title %> <%= link_to 'Edit', edit_article_path %> <%= link_to 'Delete', article_path(@article), method: :delete, data: { confirm: "Delete #{@article.title}?" } %></h1>
  <p><%= @article.content %></p>
</div>
<%= link_to 'Back to list', articles_path %>
